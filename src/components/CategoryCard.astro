---
// Componente para mostrar una tarjeta de categoría
const { nombre, imagen, ruta } = Astro.props;
---

<a
  href={ruta}
  class="block bg-brand-100 rounded-xl overflow-hidden shadow-md hover:shadow-xl transition-shadow"
>
  <div class="relative aspect-square overflow-hidden">
    <img
      src={imagen}
      alt={`Categoría: ${nombre}`}
      class="w-full h-full object-cover transition-transform duration-500 hover:scale-110"
    />
    <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent"
    ></div>
    <h3
      class="absolute bottom-4 left-4 right-4 text-white text-xl font-medium text-center"
    >
      {nombre}
    </h3>
  </div>
</a>
